const {
  renderEventListenerRepository,
} = require("./event-listener-repository.snippet");
const { renderEventListener } = require("./event-listener.snippet");

module.exports = {
  renderEventListener,
  renderEventListenerRepository,
};
