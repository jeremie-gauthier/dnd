{
  "name": "dnd",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "start:dev:db": "docker-compose -f ./packages/backend/docker-compose.yml up -d --build",
    "start:dev:server": "npm run start:dev --workspace=@dnd/backend",
    "start:dev": "npm run start:dev:db && npm run start:dev:server",
    "reinstall": "rm -rf packages/backend/node_modules packages/backend/dist packages/frontend/node_modules packages/shared/node_modules packages/shared/dist && npm i",
    "publish:prettier": "npm run publish -w @dnd/prettier-config",
    "publish:shared": "npm run publish -w @dnd/shared"
  }
}
