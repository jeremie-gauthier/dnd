import { Map } from "../map/map";

export class GameLoop {
  private isRunning: boolean;

  constructor(private readonly map: Map) {}

  private rollInitiative() {
    const initiativeScores = [1, 2, 3, 4, 5];
		for 
  }
}
